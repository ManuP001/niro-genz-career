services:
  - type: web
    name: niro-career
    env: static
    buildCommand: npm install && npm run build
    staticPublishPath: ./dist
    envVars:
      - key: VITE_ANTHROPIC_API_KEY
        sync: false
